node{
  def mvnHome
  stage('prepare') {
                   echo "cloning from SCM"
                   }
  stage('Build') {
                 echo "build using maven"
                 }
  stage('test') {
                echo "testing"
                }
  stage('Nexus') {
                echo "upload the artifact to nexus"
                }
  stage('deploy') {
                  echo "deploy to tomcat"
                  }


}
